# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build Backend_Developer`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t Backend_Developer`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system Backend_Developer && \
          adduser --system -G Backend_Developer Backend_Developer

COPY dist/apps/Backend_Developer Backend_Developer/
RUN chown -R Backend_Developer:Backend_Developer .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix Backend_Developer --omit=dev -f install

CMD [ "node", "Backend_Developer" ]
